import { InstrumentGallery } from './instrument-gallery.model';

describe('InstrumentGallery', () => {
  it('should create an instance', () => {
    expect(new InstrumentGallery()).toBeTruthy();
  });
});
